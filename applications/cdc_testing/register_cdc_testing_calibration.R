

register.transmute.calibration.info(transmute.code = 'cdct.tx',
                                    from.version = 'ehe',
                                    to.version = 'cdct',
                                    n.iter.first.sim = 1000,
                                    n.iter.subsequent.sims = 100,
                                    likelihood.instructions = test.cdc.test.positivity.likelihood.instr,
                                    prior.distribution = CDC.TESTING.PARAMETERS.PRIOR,
                                    sampling.blocks = CDC.TESTING.PARAMETER.SAMPLING.BLOCKS)


