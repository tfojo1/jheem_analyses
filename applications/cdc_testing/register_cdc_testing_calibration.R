

register.transmute.calibration.info(code = 'cdct.tx',
                                    from.version = 'ehe',
                                    to.version = 'cdct',
                                    n.iter.first.sim = 1000,
                                    n.iter.subsequent.sims = 100,
                                    likelihood.instructions,
                                    prior.distribution,
                                    sampling.blocks)